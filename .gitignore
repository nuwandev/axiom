# Go
/bin/
*.exe
*.test
*.out
vendor/

# Release build output (scripts/build-release.sh) — attached to GitHub
# Releases, not committed to the repo.
/dist/

# Local secrets/certs — never commit real material
*.pem
*.key
*.crt
/etc/axiom/
certs/

# Editor/OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Local runtime artifacts
/data/
*.log

# Private engineering artifacts — local source-of-truth only, never committed.
# Do NOT delete these files; they stay on disk for development use.
/docs/SPEC.md
/CLAUDE.md
/docs/decisions/
